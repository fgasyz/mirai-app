import 'package:flutter/material.dart';
import 'package:get/get.dart';

class GalleryImagePicker extends StatelessWidget {
  const GalleryImagePicker({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
